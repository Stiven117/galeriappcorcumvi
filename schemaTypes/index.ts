// schemaTypes/index.ts

import { galleryImageType } from './galleryImageType'

// Exportamos directamente el array, que es lo que el archivo de configuración espera.
export const schemaTypes = [galleryImageType]